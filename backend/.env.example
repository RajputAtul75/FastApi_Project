MONGODB_URI=mongodb://localhost:27017
MONGODB_DATABASE=nyayaai
GEMINI_API_KEY=
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Admin dashboard sign-in (POST /api/auth/login).
# If these are left unset the backend falls back to admin / admin123 —
# fine for local development, but set real values before any shared demo.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=
